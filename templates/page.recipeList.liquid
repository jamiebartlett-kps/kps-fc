{% comment %}
  Template Name: Recipe
  Template Type: page
{% endcomment %}

{{ 'recipeList.css' | asset_url | stylesheet_tag }}

    <header>
        <div class="header-content">
            <h1>Recipes</h1>
            <p class="subtitle">Discover delicious recipes using our freshest seasonal produce</p>
        </div>
    </header>

    <div class="container">
        <div class="filters" id="filters"></div>
        <div class="grid" id="grid"></div>
    </div>

    <script src="{{ 'recipeList.js' | asset_url }}" async></script>

    {% comment %} <script>
        // Recipe data - replace with your actual data
        const recipesData = [
            {
                id: 1,
                title: "Roasted Winter Vegetables",
                description: "Seasonal root vegetables roasted with herbs and garlic",
                image: "/placeholder.svg?height=200&width=280",
                prepTime: "15 mins",
                servings: "4",
                season: "winter",
                tags: ["Vegetarian", "Gluten-free"]
            },
            {
                id: 2,
                title: "Cauliflower Egg Curry",
                description: "Creamy curry with cauliflower and soft-boiled eggs",
                image: "/placeholder.svg?height=200&width=280",
                prepTime: "30 mins",
                servings: "2",
                season: "winter",
                tags: ["Vegetarian"]
            },
            {
                id: 3,
                title: "Beetroot & Walnut Salad",
                description: "Earthy beetroot with crunchy walnuts and goat cheese",
                image: "/placeholder.svg?height=200&width=280",
                prepTime: "10 mins",
                servings: "2",
                season: "winter",
                tags: ["Vegetarian", "Vegan"]
            },
            {
                id: 4,
                title: "Parsnip Soup",
                description: "Silky smooth parsnip soup with crispy sage",
                image: "/placeholder.svg?height=200&width=280",
                prepTime: "25 mins",
                servings: "4",
                season: "winter",
                tags: ["Vegetarian", "Vegan"]
            },
            {
                id: 5,
                title: "Kale & Chickpea Stew",
                description: "Hearty winter stew packed with greens and legumes",
                image: "/placeholder.svg?height=200&width=280",
                prepTime: "35 mins",
                servings: "4",
                season: "winter",
                tags: ["Vegan"]
            },
            {
                id: 6,
                title: "Carrot & Ginger Soup",
                description: "Warming soup with a hint of ginger and turmeric",
                image: "/placeholder.svg?height=200&width=280",
                prepTime: "20 mins",
                servings: "4",
                season: "winter",
                tags: ["Vegetarian", "Vegan"]
            }
        ];

        // Get unique seasons for filters
        const seasons = [...new Set(recipesData.map(r => r.season))];

        // Render filters
        function renderFilters() {
            const filtersContainer = document.getElementById('filters');
            const allBtn = document.createElement('button');
            allBtn.className = 'filter-btn active';
            allBtn.textContent = 'All Recipes';
            allBtn.onclick = () => renderGrid(recipesData);
            filtersContainer.appendChild(allBtn);

            seasons.forEach(season => {
                const btn = document.createElement('button');
                btn.className = 'filter-btn';
                btn.textContent = season.charAt(0).toUpperCase() + season.slice(1);
                btn.onclick = () => {
                    document.querySelectorAll('.filter-btn').forEach(b => b.classList.remove('active'));
                    btn.classList.add('active');
                    const filtered = recipesData.filter(r => r.season === season);
                    renderGrid(filtered);
                };
                filtersContainer.appendChild(btn);
            });
        }

        // Render grid
        function renderGrid(recipes) {
            const grid = document.getElementById('grid');
            grid.innerHTML = '';

            recipes.forEach(recipe => {
                const card = document.createElement('a');
                card.href = `/recipes/${recipe.id}`;
                card.className = 'recipe-card';

                card.innerHTML = `
                    <img src="${recipe.image}" alt="${recipe.title}" class="recipe-image">
                    <div class="recipe-content">
                        <h3 class="recipe-title">${recipe.title}</h3>
                        <p class="recipe-description">${recipe.description}</p>
                        <div>
                            ${recipe.tags.map(tag => `<span class="tag">${tag}</span>`).join('')}
                        </div>
                        <div class="recipe-meta">
                            <div class="meta-item">
                                <span class="meta-icon">⏱</span>
                                <span>${recipe.prepTime}</span>
                            </div>
                            <div class="meta-item">
                                <span class="meta-icon">👥</span>
                                <span>${recipe.servings}</span>
                            </div>
                        </div>
                    </div>
                `;

                grid.appendChild(card);
            });
        }

        // Initialize
        renderFilters();
        renderGrid(recipesData);
    </script> {% endcomment %}